#include<stdio.h>
#include<conio.h>

void main()
{
	float radius, area, circumference;

	printf("\nEnter the radius of the circle:");
	scanf("%d",&radius);

	printf("\nArea of tha circle = %.2d",area);
	printf("\nCircumference of the circle = %.2d",circumference);
	getch();
}