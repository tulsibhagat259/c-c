#include <stdio.h>
#include <conio.h>

void main() 
{
    char ch;
    clrscr();
    int ascii;

    printf("Enter a character: ");
    scanf("%c", &ch);

    ascii = ch;   

    printf("ASCII value of %c is: %d", ch, ascii);

    getch();    
}